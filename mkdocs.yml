site_name: arxiv2026_zoom_in
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: arxiv2026_zoom_in
      handlers:
        python:
          options:
            docstring_style: numpy